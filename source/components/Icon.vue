import { h, type FunctionalComponent } from 'vue'; interface IconProps { className?: string; } //
Helper to reduce boilerplate const createIcon = (paths: any[]): FunctionalComponent
<IconProps></IconProps>
